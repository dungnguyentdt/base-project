export const LocalStorageKeys = {
  USER: "user",
} as const;

export const SessionStorageKeys = {
  CURRENCY: "currency",
  WEIGHT_UNIT: "weight_unit",
} as const;

export const CookiesStorageKeys = {
  ACCESS_TOKEN: "access_token",
  THEME: "theme",
  LANGUAGE: "language",
} as const;
